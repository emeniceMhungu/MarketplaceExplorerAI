import { ScreenStub } from "@/components/screen-stub";

export default function ProfileScreen() {
  return (
    <ScreenStub
      title="Profile"
      description="Phase 1 profile route stub for top-level flat-route navigation validation."
    />
  );
}
